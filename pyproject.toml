[project]
name = "mlflow-features"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain-core>=1.2.16",
    "langchain-ollama>=1.0.1",
    "mlflow>=3.10.0",
    "pydantic>=2.12.5",
]

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
]
